node_modules/
target/
.anchor/
dist/
.env
test-ledger/
*.log

# Secrets & local artifacts — never commit
*id.json
keeper/.demo-state.json

# devnet treasury keypair — never commit (Vercel env only)
keeper/.treasury.json
